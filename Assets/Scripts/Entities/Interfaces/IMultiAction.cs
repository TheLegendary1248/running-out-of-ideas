using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// An interface for entities with multiple choices for their actions
/// </summary>
public interface IMultiAction 
{
    //Pool of actions
    //Evaluate Weights Function
    //Etc, etc, I donno how far to go with my planning here

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
